/**
 * @param {number[]} height
 * @return {number}
 */

// я бы попробовал пройтись по массиву и найти самые большие крайние линии
var maxArea = function(height) {
  var startIndex = 0;
  var endIndex = 0;

  for (var i = 0; i < height.length; i++) {
    
  }
};

console.log(maxArea([1, 8, 6, 2, 5, 4, 8, 3, 7])); // 49
console.log(maxArea([1, 1])); // 1
