// якась фігня
var convert = function(s, numRows) {
    

    for (let letter of s) {
        console.log(letter);
    }
};

const s = "PAYPALISHIRING", numRows = 3

console.log(convert(s, numRows));
